# Copy to infrastructure/infra-erp/.env (gitignored) and fill in real values
# before running `docker compose up -d` in this directory. These are the
# credentials each BC's .env (AUTH_DB_*, REDIS_*, RABBITMQ_*, etc. at the
# repo root) must match.

POSTGRES_DB=mydb
POSTGRES_USER=devhis
POSTGRES_PASSWORD=
POSTGRES_PORT=5433

REDIS_PASSWORD=
REDIS_PORT=6333

RABBITMQ_USER=pp
RABBITMQ_PASSWORD=
RABBITMQ_PORT=5666
RABBITMQ_MGMT_PORT=15672

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

GOTENBERG_PORT=3009
