/**
 * Swagger/Scalar documentation strings for `AuthController`.
 */

export const LOGIN_SUMMARY =
  'Login with username + password — issues JWT access/refresh tokens';
export const REFRESH_SUMMARY =
  'Exchange a refresh token for a new access/refresh token pair';
export const LOGOUT_SUMMARY =
  'Revoke the current session (and refresh token, if provided)';
export const ME_SUMMARY = 'Get the current authenticated session';
export const SET_CREDENTIAL_SUMMARY =
  'Create/reset a user credential (admin flow, after iam user creation)';
