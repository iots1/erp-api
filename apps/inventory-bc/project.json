{
  "name": "inventory-bc",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/inventory-bc/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest build inventory-bc"
      },
      "outputs": ["{workspaceRoot}/dist/apps/inventory-bc"]
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest start inventory-bc --watch | pino-pretty -c -t -l"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/inventory-bc/jest.config.js",
        "passWithNoTests": true
      }
    },
    "test:e2e": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/inventory-bc/test/jest-e2e.json",
        "passWithNoTests": true
      }
    }
  }
}
