{
  "name": "sales-bc",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/sales-bc/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest build sales-bc"
      },
      "outputs": ["{workspaceRoot}/dist/apps/sales-bc"]
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest start sales-bc --watch | pino-pretty -c -t -l"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/sales-bc/jest.config.js",
        "passWithNoTests": true
      }
    },
    "test:e2e": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/sales-bc/test/jest-e2e.json",
        "passWithNoTests": true
      }
    }
  }
}
