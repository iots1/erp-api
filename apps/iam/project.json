{
  "name": "iam",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/iam/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest build iam"
      },
      "outputs": ["{workspaceRoot}/dist/apps/iam"]
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest start iam --watch | pino-pretty -c -t -l"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/iam/jest.config.js",
        "passWithNoTests": true
      }
    },
    "test:e2e": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/iam/test/jest-e2e.json",
        "passWithNoTests": true
      }
    }
  }
}
