{
  "name": "supplier-bc",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/supplier-bc/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest build supplier-bc"
      },
      "outputs": ["{workspaceRoot}/dist/apps/supplier-bc"]
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest start supplier-bc --watch | pino-pretty -c -t -l"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/supplier-bc/jest.config.js",
        "passWithNoTests": true
      }
    },
    "test:e2e": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/supplier-bc/test/jest-e2e.json",
        "passWithNoTests": true
      }
    }
  }
}
